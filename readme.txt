Steps to use the tool:

- Install the required packages mentioned in requirements.txt
- Run the app in the command terminal using the command "streamlit run main.py"
- Once the tool is opened in web browser, the blogs that you want to search
- In the question bar enter you question to get the required Answer
